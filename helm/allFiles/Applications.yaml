apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: resource-app
  namespace: argocd
spec:
  # project: default
  project: resource-project
  source:
    repoURL: https://github.com/Riteshatri/resource-management-backend-resource-service-microservice
    targetRevision: main
    path: heml/resource
  destination:
    server: https://kubernetes.default.svc
    namespace: resource-mgmt
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# kubectl apply -f .\Applications.yaml
# kubectl get applications -n argocd
# kubectl get pods -n resource-mgmt
# kubectl get svc -n resource-mgmt